name: pro2
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.10.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.8.1
  firebase_auth: ^5.3.4
  cloud_firestore: ^5.6.0
  flutter_riverpod: ^2.6.1
  cupertino_icons: ^1.0.5
  google_maps_flutter: ^2.5.3
  google_maps_flutter_web: ^0.5.4+3
  qr_flutter: ^4.1.0
  share_plus: ^7.2.1
  screenshot: ^3.0.0
  path_provider: ^2.1.1
  printing: ^5.11.0
  pdf: ^3.10.7
  mobile_scanner: ^3.5.2 # أضفنا مكتبة المسح الضوئي
  shared_preferences: ^2.5.5
  permission_handler: ^12.0.1
  url_launcher: ^6.1.12
  pay: ^3.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/apple_pay_profile.json
